"$schema" = "https://raw.githubusercontent.com/byteowlz/schemas/refs/heads/main/sx/sx.config.schema.json"

# sx configuration file
# Copy to $XDG_CONFIG_HOME/search-engine/config.toml
# (typically ~/.config/search-engine/config.toml).
# A legacy ~/.config/sx/config.toml is still read (read-only) if present.

# Primary search engine (searxng, exa, jina, brave, tavily)
engine = "searxng"

# Automatic fallback is OFF by default. Leave this empty to never fall back.
# Only the engines you list here are tried (in order) when the primary fails.
fallback_engines = []
# Example opt-in (free/self-hosted engines only):
# fallback_engines = ["jina", "brave"]

# Paid backends (tavily, exa API) are NEVER used in automatic fallback unless
# you explicitly opt in below. This protects you from silently spending credits.
allow_paid_fallback = false
# To allow paid fallback you must BOTH set this true AND list the paid engine:
# allow_paid_fallback = true
# fallback_engines = ["jina", "brave", "tavily"]

# Primary SearXNG instance URL (required when engine = "searxng")
searxng_url = "https://searxng.example.com"

# Additional SearXNG instances for failover (optional)
searxng_urls = [
  "https://searxng-backup-1.example.com",
  "https://searxng-backup-2.example.com"
]

# Multi-instance strategy: ordered, parallel-fastest (default: ordered)
searxng_strategy = "ordered"

# Optional basic authentication for SearXNG
# searxng_username = "username"
# searxng_password = "password"

# Number of results to show per page (default: 10)
result_count = 10

# Default safe search level: none, moderate, strict (default: strict)
safe_search = "strict"

# Show complete URLs in search results (default: false)
expand = false

# HTTP method for SearXNG requests: GET or POST (default: GET)
http_method = "GET"

# Request timeout in seconds (default: 30.0)
timeout = 30.0

# Disable SSL certificate verification (default: false)
no_verify_ssl = false

# Disable user agent header (default: false)
no_user_agent = false

# Disable colored output (default: false)
no_color = false

# Enable debug output (default: false)
debug = false

# Default output mode (optional, set to "interactive" to default to interactive mode)
# default_output = ""

# Query history
history_enabled = true
max_history = 100

# Default search categories (optional)
# Available: general, news, videos, images, music, map, science, it, files, social+media
# categories = ["general", "news"]

# Default SearXNG search engines (optional)
# engines = ["duckduckgo", "google", "brave"]

# Default search language (optional)
# language = "en"

# URL handler command (optional, auto-detected by default)
# macOS: "open", Linux: "xdg-open", Windows: "explorer"
# url_handler = "open"

# Exa Search (API or MCP)
[engines_exa]
mode = "auto"                 # auto, api, mcp
api_key = ""                  # optional, or set EXA_API_KEY env var
# base_url = "https://api.exa.ai/search"  # override the Exa API endpoint
mcp_url = ""                  # optional MCP endpoint URL (e.g. https://mcp.exa.ai/mcp)
mcp_tool = "exa-web-search"   # MCP tool to call
num_results = 10

# Jina Search (keyless or API key)
[engines_jina]
api_key = ""                  # optional, or set JINA_API_KEY env var
allow_keyless = true
base_url = "https://s.jina.ai"

# Brave Search API (https://api.search.brave.com/)
# Free tier: 2,000 requests/month
[engines_brave]
api_key = ""                  # optional, or set BRAVE_API_KEY env var
# base_url = "https://api.search.brave.com/res/v1/web/search"  # override endpoint

# Tavily Search API (https://tavily.com/) — PAID backend
# Free tier: 1,000 credits/month. Each search consumes >= 1 credit.
[engines_tavily]
api_key = ""                  # optional, or set TAVILY_API_KEY env var
# base_url = "https://api.tavily.com/search"  # override endpoint
search_depth = "basic"        # basic (1 credit) or advanced (2 credits)
include_raw_content = false   # return full page content with results
include_answer = false        # return a direct answer
